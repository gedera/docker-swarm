.agents/
# Symlink a ../.agents/skills (no versionar)
.claude/skills
# Variables de entorno
.env
# Reportes efímeros (critias, mvf, etc.)
/tmp/
# Perfil local + runs de multi-vendor-feedback (paths de máquina)
/vendors.local.yaml
/mvf-runs/
# Gemas construidas por el flujo de release
*.gem
